import React from 'react'
import ChartReviewContainer, { mapDispatchToProps, mapStateToProps } from '../../../../src/routes/ChartReview/containers/ChartReviewContainer'
import expect from 'expect'

describe('Chart review container', () => {
  beforeEach(function () {
        localStorage.logged_user_role = 'admin'
    });

})